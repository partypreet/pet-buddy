import React from 'react';
import ReactDOM from 'react-dom/client';
import PetBuddy from './BunnyBuddy';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PetBuddy />
  </React.StrictMode>
);
